# DEVELOPMENT RULES

Never break existing logic.

Never remove GPRID.

Always preserve AI integration.

Always build after changes.

Always run lint.

Always use reusable components.

Never duplicate code.

Never hardcode strings.

Use copy.ts.

Support mobile first.

Never reduce accessibility.

Never introduce TypeScript errors.